import os
f = open('data.txt', 'w')
f. write("Hello! How are you")
f.close()

#if we don`t give the file name it create a new, we can create a new file and add new data
# append means add new data after add
